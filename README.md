# Tiny Mines

A compact minesweeper-style logic puzzle built with Flutter for iOS, Android, and web.

## Local Web Build

```sh
flutter build web --release --base-href /TinyMines/
```

GitHub Pages deploys from `.github/workflows/deploy-pages.yml`.
